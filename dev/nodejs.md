# NodeJS

## Windows

Install nvm and NodeJS
- download `nvm-setup.exe` file from https://github.com/coreybutler/nvm-windows/releases
- open a new powershell terminal

```powershell
nvm install lts
nvm use lts
node --version
```
